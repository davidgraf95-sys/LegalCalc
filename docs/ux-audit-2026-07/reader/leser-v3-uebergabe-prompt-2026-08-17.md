Nutze den Skill `bauschritt` für den ganzen Session-Zyklus. Schritt: W2·5m-LESER-V3 (Dach-Schritt «Gesetz-Leser V3: Hülle neu, Kern unangetastet»). Arbeite als Orchestrator: du planst, koordinierst und landest; Heavy Lifting (Bau, Prüfung, Recherche, Screens) an viele Unteragenten; token-sparsam (Slices statt Volltexte, kompakte Rückgaben).

Auftrag David (Dauer-Go, Wortlaut 16.8.: «jetzt bau bis ich stop sage» → «bau bis h4»): Baue die Etappen des Plans `fahrplaene/FAHRPLAN-LESER-V3.md` in dieser Reihenfolge, je eigener PR: **H2 landen → H2b (Ästhetik-Nachzug) → H3 (Panel) → S1 (Historie zweiwertig) → S2-Bildbogen (Ja am Bild) → alle H4-Umschalt-Kriterien erfüllen und den Kontaktbogen erstellen — HALT vor dem H4-Umschalten selbst (Davids Ja).**

Einstieg (Pflicht, in dieser Reihenfolge, Slices lesen):
1. `docs/ux-audit-2026-07/reader/leser-v3-handoff-2026-08-17.md` (Übergabe: Stand, offene Etappen, Regeln, Wartepunkte)
2. `npm run fahrplan -- fahrplaene/FAHRPLAN-LESER-V3.md 7` (Etappen + Vollzug), dann Kap. 9 (Entscheide F1–F8/D-A–D-C), Kap. 12 (Abnahmekriterien), Kap. 14 (Verhältnis zu anderen Schritten)
3. `docs/ux-audit-2026-07/reader/leser-v3-design-grundlage.md` Kap. 0–8 (Design-Massstab)
4. Memory `leser-v3-dauer-go-2026-08-16` und STRUKTUR-Karte «Session 16./17.8.2026»
5. `npm run plan:next` (Kollisionen; Worktree `/Users/david/Developer/LexMetrik-h2` mit Branch `feat/leser-v3-h2` = PR #539 ist bewusst stehen gelassen)

Vor H2b: QS-AUTOMATIK-Position «Vercel-CI-Deploy (b)» als eigener PR bauen — Secret `VERCEL_TOKEN` liegt und ist geprüft (Actions-Run 31977204874: whoami davidgraf95-1010, Projekt lexmetrik sichtbar): CI-Job auf main NACH grünen Toren (`vercel pull --environment=production` · `vercel build --prod` · `vercel deploy --prebuilt --prod`; Team `team_jfUx2xb2Jo3RVYUFYsIW94lG`, Projekt `prj_1xdIoeM6YjmelXq6DSMpywDOXq2x`); `vercel.json` `"git": {"deploymentEnabled": false}` (Vercel deployt nichts mehr von selbst, Git-Verbindung bleibt); Ignore-Skript + Prod-Stand-Wächter bleiben als Sicherung; Rot-Beweis (Deploy-Job ohne Token rot); Landung-Skill Nachkontrolle Ziff. 1 nachziehen; erster CI-Deploy = Live-Gang von S3/H2 (Vercel-Tageslimit läuft dann nicht mehr an).

Sofortiger erster Handgriff — H2 landen: PR #539 (`feat/leser-v3-h2`, Worktree `LexMetrik-h2`; S3 ist dort per Merge-Commit aufgenommen, Diagnose «Shard-7-Rot ist kein H2-Defekt» = Ä24; gepushter HEAD `a3f59209d`, `npm run gate` grün, 61 V3-Specs grün; `leser-ohne-gliederungslinie:71` bleibt unter Last flaky — Wurzel createRoot→hydrateRoot ist QS-PERF-Position, nicht H2). Prüfe `git status`/`git log` im Worktree, fahre die Tore nackt (`npm run gate` · `check:testtreue` · `check:e2e-shards` · build + `check:perf-budget` · `npx playwright test e2e/leser-v3-*.e2e.ts e2e/leser-kopf-*.e2e.ts --project=chromium` · `--project=leser-v3`), pushe (sparsam), CI abwarten (Hintergrund-Bash mit `until`-Schleife, KEIN Monitor-Stream), bei Grün `gh pr merge 539 --squash` — Aufräumen NUR nach `gh pr view 539 --json state` == MERGED und in einem separaten Kommando. Vercel-Tageslimit ist ggf. noch aktiv (Prod-Deploy blockiert bis Reset; Vercel-Status ist nicht required) — Merges trotzdem; der erste main-Merge nach dem Reset löst den Deploy aus; Live-Check danach: `curl -s https://lexmetrik.vercel.app/ | grep lexmetrik-build` und `…/gesetze/bund/STPO?leser=v3`.

Feste Regeln David (nicht verhandelbar): je Etappe VOR dem Merge **drei unabhängige Prüfer** (Bug-Check §9 · Ästhetik-Prüfer mit Screens H/D/S hell/dunkel gegen Design-Grundlage + HIG, Live oder `vite preview` · Architektur-/Erlass-Neutralitäts-Prüfer: Bund/Verordnung/Kanton/Staatsvertrag ohne Sonderpfad, Abhängigkeitsrichtung Hülle→Kern, Vertrag, Erweiterungspunkte, Wartbarkeit/Änderbarkeit/Sortierung, Rückbaubarkeit) — Befunde als Nachzug in denselben PR (frischer Agent, nicht in den laufenden Bau nachschieben); Ästhetik-Checkliste Ä1–Ä24 (Fahrplan Kap. 7) bis zum Schluss führen, jeder Punkt sinnvoll umgesetzt; Bund-Probe (Gesetz + Verordnung + Staatsvertrag) und Kantons-Probe je Etappe unter `?leser=v3`; Split-View immer mitdenken; Suchfeld immer zugreifbar (oberstes Element im sticky Block; Ä19: im Split existiert heute KEIN Suchfeld — vor der Abnahme fixen); «Rechtsprechung im Text» aus ⇒ Zähler UND Randlasche weg; wartungsarmer, änderbarer, sortierter Code; Kern (`ArtikelLeser`/`ArtikelBody`, `src/lib`) unangetastet ausser deklarierter Ausnahme; Golden byte-gleich; Risikopfad ⇒ Skill `gegenpruefung`; Commit-Typen ehrlich (`refactor` nie mit Testdateien — check:testtreue), Commit-Messages per `-F <datei>`, Push nur bei Meilensteinen (Vercel-Limit), nie `--amend`/`stash`/`reset --hard`; §14.7-Klausel wörtlich in jeden Sub-Agenten-Auftrag; Unteragenten enden mit prüfbarer Rückgabe, nie mit «ich warte».

Entscheide David (gelten): F1 ja · F2 ja · F3 = V1 (19 px, S2 nach Bild-Ja) · F4 ja · F5 ja (gebaut, S3) · F6 nein · F7 = A (Kopf mit «Ansicht ▾») · F8 = Randlasche behalten · D-A Regler ja (Leser-eigen, gebaut) · D-B Dunkelmodus behalten (14 Rollen) · D-C Serif behalten.

Wartet auf David (nicht selbst entscheiden): H4-Umschalten (Kontaktbogen) · S2 Schriftbild (Bild) · Vercel-Wurzelfix (Dashboard/Token/Plan, ROADMAP QS-AUTOMATIK) · lokal `git config --unset core.hooksPath`.

Kommunikation mit David: Deutsch, Klartext zuerst (Jurist, Software-Laie), Statusworte erledigt · läuft · wartet auf dich · Problem; Meldung bei jedem gemergten Schritt («H2 live», …) mit Vergleichsadresse `?leser=v3`; nach jeder Etappe zusätzlich das Ästhetik-Urteil (Note) in einem Satz. Am Session-Ende: Station E (Session-Karte Kurzkarte, `struktur-rotieren.py --check`, `selbstopt:erheben`, `plan:bild`, Bau-Flächen abräumen, Sammel-Push, §17-Lehren, Memory-Hygiene) und Übergabe-Datei aktualisieren.
